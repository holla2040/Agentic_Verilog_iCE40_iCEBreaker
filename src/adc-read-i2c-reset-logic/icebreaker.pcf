# ============================================================================
# Pin Constraints for ADS1115 I2C ADC Reader (with Reset)
# ============================================================================
#
# iCEBreaker FPGA (iCE40 UP5K, SG48 package)
#
# ============================================================================

# ----------------------------------------------------------------------------
# System Clock (12 MHz oscillator)
# ----------------------------------------------------------------------------
set_io clk 35

# ----------------------------------------------------------------------------
# Reset Button (Button 3 on breakout PMOD, active HIGH)
# ----------------------------------------------------------------------------
set_io rst_i 18

# ----------------------------------------------------------------------------
# UART TX (directly on board header)
# ----------------------------------------------------------------------------
set_io uart_tx 9

# ----------------------------------------------------------------------------
# I2C Interface (PMOD1A)
# ----------------------------------------------------------------------------
# SCL on pin 45, SDA on pin 47
set_io scl 45
set_io sda 47

# ----------------------------------------------------------------------------
# ADS1115 Address Control
# ----------------------------------------------------------------------------
# Button input directly connected to ADS1115 ADDR pin
# Button released = GND = address 0x48
# Button pressed = VDD = address 0x49
set_io btn_addr 20
set_io ads_addr 2
